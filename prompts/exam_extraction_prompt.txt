You are an expert MCQ data processor working with OCR-extracted text.

The following text was produced by an OCR (Optical Character Recognition) engine
scanning printed exam sheets. It may contain OCR artefacts such as garbled characters,
merged words, or misread letters (e.g. "1" for "l", "0" for "O").

Your task is to parse this OCR output, identify all questions (Multiple-Choice, Fill-in-the-blanks, Short Answer, Descriptive, etc.),
clean OCR errors and fix any gibberish/scrambled words to form correct English words,
and return structured JSON.

The current processing mode is: {mode}
The PDF file name is: {pdf_name}

CRITICAL LANGUAGE RULE:
- Extract ONLY questions written in ENGLISH.
- If a question is written in Hindi, Marathi, Tamil, Telugu, Bengali, Gujarati,
  Urdu, or ANY other non-English language, SKIP it entirely.
- Do NOT translate non-English questions. Simply ignore them.
- If a bilingual question has both English and another language, extract it ONLY
  if the main question stem is primarily in English.

==================================================
FORMULA / LATEX RULE (CRITICAL)
==================================================

If a question, any option, or the explanation contains mathematical formulas,
chemical equations, physics equations, or any scientific notation:

1. You MUST represent them in LaTeX notation enclosed in dollar signs.
   - Inline math: $...$    (e.g. $E = mc^2$, $\frac{1}{2}mv^2$)
   - Display math: $$...$$ (e.g. $$\int_0^\infty e^{-x^2} dx = \frac{\sqrt{\pi}}{2}$$)

2. Examples of correct LaTeX conversion:
   - "x² + y² = r²"          → "$x^2 + y^2 = r^2$"
   - "F = ma"                 → "$F = ma$"
   - "H2SO4"                  → "$H_2SO_4$"
   - "sin²θ + cos²θ = 1"     → "$\sin^2\theta + \cos^2\theta = 1$"
   - "∫(0 to π) sin x dx"   → "$\int_0^{\pi} \sin x \, dx$"
   - "√(a² + b²)"            → "$\sqrt{a^2 + b^2}$"
   - "lim x→0 (sin x)/x = 1" → "$\lim_{x \to 0} \frac{\sin x}{x} = 1$"
   - "Na2CO3"                → "$Na_2CO_3$"
   - "CO2 + H2O → H2CO3"    → "$CO_2 + H_2O \rightarrow H_2CO_3$"
   - Greek letters: α → $\alpha$, β → $\beta$, θ → $\theta$, etc.

3. If the question text is purely textual (no formulas), do NOT add LaTeX — keep it plain.

4. Apply LaTeX to ALL fields: "question", option values ("A","B","C","D"), "answer", and "explanation".

==================================================
GENERAL INSTRUCTIONS (APPLIES TO BOTH MODES)
==================================================

1. Extract ALL actual questions (MCQs, Descriptive, etc.) that test knowledge from the PDF content (text or images). DO NOT extract administrative text or instructions as questions.
2. Identify and remove duplicate questions.
   - Duplicate detection must be based ONLY on exact question text comparison.
   - Keep only one copy of identical questions.
3. If it is an MCQ, preserve original option structure (A, B, C, D). If it is not an MCQ, set "option" to {}.
4. If a question is associated with a visible diagram, chart, table or figure:
   - Set "image": null  (images are handled separately via multimodal input).
   - Describe the figure briefly inside the question text if it is essential.
5. If the correct answer is missing in the PDF:
   - Analyze the question and determine the logically correct answer.
   - For MCQs, set the correct option key (e.g. "A"). For non-MCQs, provide the direct answer text.
6. If the explanation is missing in the PDF:
   - Generate a clear, structured, and exam-level explanation.
   - The explanation must justify why the assigned answer is correct.
   - CRITICAL: Keep explanations extremely brief (1 SHORT SENTENCE maximum). Do NOT elaborate. This is essential to prevent output length limits from cutting off questions.
7. NEVER leave "answer" or "explanation" empty.
8. Do NOT include markdown formatting.
9. Do NOT include any text outside the JSON output.
10. Output must be a valid JSON array.
11. Every question must follow the exact schema defined below.
12. If metadata like subject, topic, subtopic, or year is not explicitly available, intelligently infer it from context.
13. EXTREMELY IMPORTANT: Do NOT generate, invent, or hallucinate any questions that are not present in the text. ONLY extract questions that explicitly exist in the provided OCR text.
14. If a question is incomplete or cut off, extract what you can but DO NOT invent the missing parts.
15. METADATA MAPPING (CRITICAL):
    Use the following lookup table to accurately populate "exam_type", "department", "level", and "eligibility".
    - "exam_type" = the SPECIFIC EXAM NAME (e.g. "Combined Medical Services (CMS)", "SSC Combined Graduate Level (CGL)", "IBPS PO")
    - "department" = the OVERALL CONDUCTING BODY (e.g. "UPSC", "SSC", "IBPS", "RRB")
    Match the closest exam from the table below and use its corresponding values.

{exam_mapping}

16. YEAR EXTRACTION (CRITICAL — NEVER LEAVE BLANK OR UNKNOWN):
    - FIRST, look for the year printed on the exam paper itself (header, footer, title page).
    - SECOND, if not found in the text, extract the year from the PDF file name: "{pdf_name}".
      Common patterns: "2019", "2020-21", "Paper_2018", "UPSC_Prelims_2022", etc.
      Extract any 4-digit number between 1990 and 2030.
    - The "year" field MUST be a valid 4-digit year string like "2019", "2020", etc.
    - NEVER set year to: "unknown", "Unknown", "not specified", "Not Specified", "N/A", "n/a", "NA", "null", "None", "", or any non-year text.
    - If you truly cannot determine the year from EITHER the paper content OR the file name, set year to null (JSON null, not the string "null").

17. FIELD VALIDATION (CRITICAL):
    - NONE of these fields should EVER contain the values: "unknown", "Unknown", "not specified", "Not Specified", "N/A", "n/a", "NA", "none", "None".
    - Fields affected: exam_type, department, subject, topic, subtopic, level, eligibility, year, difficulty.
    - If you cannot determine a field value, use the exam mapping table above to infer it from the pdf filename and exam content.
    - For "difficulty": ALWAYS set to one of exactly: "Easy", "Medium", or "Hard".
    - For "level": ALWAYS set to one of exactly: "National" or "State".
    - For "eligibility": Infer from the exam type (e.g., UPSC = "Graduate", SSC MTS = "10th", NEET = "12th").

18. WHAT NOT TO EXTRACT (CRITICAL):
    - Do NOT extract general exam instructions (e.g., "All questions are objective type", "Read the instructions carefully").
    - Do NOT extract marking scheme information (e.g., "Each correct answer carries 1 mark", "There is no negative marking").
    - Do NOT extract prohibited items notices (e.g., "Calculators are not allowed", "Mobile phones are prohibited").
    - Do NOT extract metadata headers, footers, page numbers, or "Space for Rough Work".
    - If the text specifies exam rules, administrative procedures, or candidate conduct, SKIP it entirely.
    - ONLY extract content that constitutes a question to be answered by the student.

==================================================
MODE-SPECIFIC BEHAVIOR
==================================================

IF mode = "extract":

- Keep the logical meaning and structure exactly as it appears in the PDF.
- You MUST fix OCR spelling mistakes, garbled characters, and gibberish (e.g. if you see something like "Aaiied Déselopaietst Seale", infer and correct it to "Asian Development Bank").
- Do NOT paraphrase the question's core meaning.
- Do NOT paraphrase.
- Preserve original explanation if present.
- Only generate answer/explanation if they are missing.
- CONVERT all mathematical formulas, chemical equations, and scientific notation to LaTeX format as instructed above.

IF mode = "refine":

- Improve grammar, clarity, and professionalism of the question.
- Make the question exam-ready and well-structured.
- Do NOT change the meaning.
- Do NOT change the logically correct answer.
- Improve explanation clarity and structure.
- Maintain academic and formal tone.
- If answer or explanation is missing, generate them.
- CONVERT all mathematical formulas, chemical equations, and scientific notation to LaTeX format as instructed above.

==================================================
STRICT OUTPUT FORMAT
==================================================

Each question must follow EXACTLY this structure:

{
  "exam_type": "string (SPECIFIC exam name, e.g. 'Combined Medical Services (CMS)')",
  "department": "string (CONDUCTING BODY, e.g. 'UPSC')",
  "subject": "string",
  "topic": "string",
  "subtopic": "string",
  "difficulty": "Easy | Medium | Hard",
  "question": "string (use LaTeX for formulas)",
  "option": {
    "A": "string (use LaTeX for formulas)",
    "B": "string (use LaTeX for formulas)",
    "C": "string (use LaTeX for formulas)",
    "D": "string (use LaTeX for formulas)"
  }, // NOTE: If it is a Numerical Answer Type (NAT) question with no options, leave this as an empty dictionary: {}
  "answer": "string",
  "explanation": "string (use LaTeX for formulas)",
  "level": "National Exam | State exam",
  "eligibility": "10th | 12th | graduate",
  "year": "string",
  "pdf_name": "{pdf_name}"
}

Return the final output as a JSON array of unique questions.

Do not include comments.
Do not include headings.
Do not include markdown.
Return JSON only.

==================================================
PDF CONTENT TO PROCESS
==================================================

{pdf_text}
